# Mock-up / Placeholder Audit

This document lists locations in the codebase that include mockups, placeholders, demo values, or fallback heuristics that should be replaced or reviewed before production.

| File | Location / symbol | Mock-up type | Evidence / placeholder text (snippet) | Recommended action |
|---|---:|---|---|---|
| app.py | Route: `/api/files/<id>/summarize` | Placeholder stub | "# AI Summarization (placeholder – swap body for real AI call)" / returns: "AI summary not yet configured. Replace _run_ai_summary() in app.py with your AI agent call." | Implement real LLM call or wire configured LLM; remove stub message. |
| app.py | `_run_ai_flashcards` | Heuristic fallback | Attempts `chat(...)`; on LLM error falls back to local heuristics (German cards). | Configure/enable LLM or replace fallback with production behavior. |
| app.py | `_run_ai_group_topics` | Placeholder output | Fallback: "# General\n- [ ] No file summaries available yet. Generate summaries for each file first." | Ensure file summaries / LLM integration; improve UX for empty state. |
| llm.py | `_DEFAULT_ENDPOINT` | Demo / hardcoded | Default URL: `https://demollmleon.cognitiveservices.azure.com/.../gpt-4o/...` | Remove demo URL; require AZURE_OPENAI_ENDPOINT or document secure config. |
| .env | project root | Env placeholders | "# Replace the placeholder values with your actual credentials." | Provide example values in docs and emphasize secret management; keep file out of VCS. |
| doc/tasks.md | tests guidance | Test mock guidance | "Add unit tests mocking llm.chat for summarization and flashcards endpoints" | Keep tests; implement mocks in test suite. |
| static/js/app.js, templates/index.html | UI placeholders | UI placeholder text | e.g. "Summary will appear here…", "Add a few sample tasks if you have them.", input placeholder "e.g. Machine Learning" | Cosmetic only — review copy for production if desired. |


## Notes & Next Steps

- Most functional "mock" items are intentionally deliberate fallbacks when the LLM isn't configured. The highest-priority changes are:
  1. Remove or replace the demo endpoint in llm.py and require explicit AZURE_OPENAI_ENDPOINT configuration.
  2. Wire a true LLM integration for _run_ai_summary/_run_ai_flashcards or document how to enable it.
  3. Add unit/integration tests that mock `llm.chat` and `llm.chat_completion` per docs.

- If desired, I can open a PR implementing (1) and adding configuration validation errors to fail fast.

Generated by Copilot CLI audit script.
